// Accepted file types for the media library's file picker.
// Mirrors config/media.php on the backend (kept in sync manually — the
// backend is the real enforcement; this just filters the picker).
export const MEDIA_ACCEPT = 'image/*,video/mp4,application/pdf,.doc,.docx,.xls,.xlsx';